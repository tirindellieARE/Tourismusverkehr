# This repo tracks code and documentation only. Raw inputs, intermediary data,
# and model outputs are all reproducible by running the scripts (see
# tourists/README_script_map.md for the exact pipeline and required initial
# inputs) and are not committed -- some individual files run into the hundreds
# of MB, which git handles poorly.
tourists/data/
tourists/results_output/
